# CReated two css files with modification done on Body element


git add conflict.css
git commit -m "conflit merge"

git checkout master

git merge mybranch

# conflict arrises

git pull origin master

# manually made changes to conflict.css

git add conflict.css

git commit -m "conflict resolved"

git merge mybranch