import React from 'react'

const Destructured=({id,name})=>{
    return(
        <div>
        <h3>{name} and {id} has been destructured in functional component</h3>
        </div>
    )
}
export default Destructured